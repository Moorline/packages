Project context is intentionally minimal.

- Direction and constraints live in `VISION.md`.
- Read `VISION.md` only when needed for planning/scoping to keep token usage low.
- If work conflicts with `VISION.md`, call out the mismatch and suggest an aligned alternative. Never update `VISION.md` yourself.
- Do not worry about maintaining backwards compatability. We use git.
